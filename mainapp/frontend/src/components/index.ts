import { createHistory, createRouter, Router } from "vue-router";

import Profile


const routes: any = [

    {
        path: "/:username/profile",
        name: "Profile",
        component: Profile,
    }
]
